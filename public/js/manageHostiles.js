dndApp.controller('ManageHostilesController', ['$scope', function ($scope) {

  console.log('loaded the manage hostiles controller');

  var init = function() {
    // TODO - any operations that should happen when this page is loaded
  };

  init();
}]);
